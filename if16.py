#!/usr/bin/env python3`

# create the string hostname
hostname = input("What value should we set for hastname?")
# test logic with the `if` statement
# what to do if this statement is found to be true
if hostname.upper() == "MTG":
    print("The hostname was found to be MTG\nHostname matches expected config to the screen.")

print("exiting the screen")

